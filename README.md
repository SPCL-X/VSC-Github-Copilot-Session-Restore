# VSC-Github-Copilot-Session-Restore
Extracts and reorganizes Copilot session data from JSONL files to recover lost session history when a VS Code workspace wasn’t saved. When using the tool on another PC or user environment, limit the target workspace to avoid state inconsistencies and start a new session if necessary.
